#set("title") { Posts }
#set("body") {
    <div id="main">
    <div class="posts">
    #for(post in posts) {
    <p><a href="/posts/#(post.link)">#(post.title)</a></p>
    }
    </div>
    </div>
}

#embed("master")